NinjaWebhook installed.

NEXT STEPS — NinjaTrader Add-On
1. Open NinjaTrader 8
2. Tools → Import → NinjaScript Add-On…
3. Choose WebhookTradeListener-AddOn.zip from this install folder (also copied to Desktop if you checked that option)
4. Compile if prompted, then: New → Webhook Trade Listener
5. Set Account / Instrument / Quantity → Start Listener

Webhook Receiver
- Listens on http://127.0.0.1:5088 (no secret)
- Needs NT listener on 127.0.0.1:7077
- Discord webhook (if entered during setup) is saved in config.json → flow.discord_webhook_url

Flow Chrome extension
- Point Options / Futures webhooks to http://127.0.0.1:5088/signal
- See FLOW_TRADING.md

Options trade-receiver
- Optional URL and device API key are saved under config.json → options
- Options are sent only after the flow filter and futures order both succeed
- See OPTIONS_TRADING.md

Tunnel (TradingView)
- Use ngrok/cloudflare to https → http://127.0.0.1:5088
- See start-ngrok-tunnel.cmd and DISTRIBUTION.md
